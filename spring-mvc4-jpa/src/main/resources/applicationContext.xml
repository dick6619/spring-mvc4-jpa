<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:jee="http://www.springframework.org/schema/jee" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-4.0.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx-4.0.xsd
           http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-4.0.xsd
           http://www.springframework.org/schema/aop
           http://www.springframework.org/schema/aop/spring-aop-4.0.xsd">
	<context:annotation-config />

	<!-- scan Service -->
	<context:component-scan base-package="com.iii.emp.service" />
	<context:component-scan base-package="com.iii.dept.service" />

	<!-- scan DAO -->
	<context:component-scan base-package="com.iii.emp.dao" />
	<context:component-scan base-package="com.iii.dept.dao" />

	<!-- test 要用的時候才生成實例 -->
	<bean id="empTest" class="com.iii.emp.dao.impl.EmpDAOImpl"
		lazy-init="true" />

	<!-- Enable Transaction -->
	<tx:annotation-driven transaction-manager="transactionManager" />

	<!-- Transaction Manager Config -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory" />
	</bean>

	<!-- 永續層框架 -->
	<!-- JPA 提供一個統一規範(interface) -->
	<!-- JPA Factory Config -->
	<bean id="entityManagerFactory"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">

		<!-- 1. Persistence Unit -->
		<property name="persistenceUnitName" value="spring-jpa-unit" />

		<!-- 2. JPA Vendor Adapter (use Hibernate) -->
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter" />
		</property>

		<!-- 3. Hibernate Config -->
		<property name="jpaProperties">
			<props>
				<!-- SQL方言 -->
				<prop key="hibernate.dialect">org.hibernate.dialect.MySQLDialect</prop>
				<!-- 是否顯示實際操作資料庫時的SQL -->
				<prop key="hibernate.show_sql">true</prop>
				<!-- 是否將顯示的SQL排版 -->
				<prop key="hibernate.format_sql">false</prop>
				<!-- 尚不知道用途 -->
				<!-- <prop key="hibernate.hbm2ddl.auto">update</prop> -->
				<!-- JNDI連線池 -->
				<prop key="hibernate.connection.datasource">java:/comp/env/jdbc/TestDB</prop>
			</props>
		</property>
		<!-- DataSource 測試用 -->
		<!-- <property name="dataSource" ref="dataSource" /> -->
	</bean>
	<!-- JPA Factory End -->
	<!-- 永續層框架 -->

	<!-- DataSource 測試用 -->
	<!-- <bean id="dataSource" -->
	<!-- class="org.springframework.jdbc.datasource.DriverManagerDataSource"> -->
	<!-- <property name="driverClassName" value="com.mysql.jdbc.Driver"></property> -->
	<!-- <property name="url" value="jdbc:mysql://localhost:3306/test" /> -->
	<!-- <property name="username" value="root" /> -->
	<!-- <property name="password" value="123456" /> -->
	<!-- </bean> -->

	<bean
		class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />

</beans>